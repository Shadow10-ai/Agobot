services:
  # ─── Backend (FastAPI + ML) ──────────────────────────────────────────
  - type: web
    name: agobot-backend
    runtime: python
    region: frankfurt
    rootDir: backend
    buildCommand: pip install -r requirements.prod.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT --workers 1
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: MONGO_URL
        sync: false
      - key: DB_NAME
        value: agobot_prod
      - key: JWT_SECRET
        generateValue: true
      - key: KRAKEN_API_KEY
        sync: false
      - key: KRAKEN_API_SECRET
        sync: false
      - key: PYTHON_VERSION
        value: "3.11.0"

  # ─── Frontend (React → static site) ─────────────────────────────────
  - type: web
    name: agobot-frontend
    runtime: static
    region: frankfurt
    rootDir: frontend
    buildCommand: yarn install && yarn build
    staticPublishPath: build
    plan: free
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: REACT_APP_BACKEND_URL
        value: https://agobot-backend.onrender.com
      - key: NODE_VERSION
        value: "20.20.1"
